[package]
name = "skill-flow-desktop"
version = "1.3.1"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
sys-locale = "0.3"
tauri = { version = "2", features = ["tray-icon"] }

[features]
default = []
